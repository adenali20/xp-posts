spring:
  application:
    name: xp-posts
  datasource:
    url: jdbc:mysql://dev.adenali.com:3306/devdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

un: dev
server:
  port: 8050





